---
layout: default
title: Download & Setup
description: Download Kolpa Engine and follow the quickstart guide to set up your C# game development environment.
---

<div class="page-header">
  <div class="shell">
    <h1 class="page-title">Get Started</h1>
    <p class="lede">
      Download the editor, initialize a template via command line, or compile the source.
    </p>
  </div>
</div>

<div class="shell main-download-content">
  <!-- Grid for Setup & Downloads -->
  <div class="download-grid">
    <!-- Download Packages -->
    <div class="download-card">
      <div class="dl-badge">v0.8.0-beta</div>
      <h2>Engine Editor</h2>
      <p class="dl-desc">
        Includes the complete visual editor, compiler toolchain, and default 3D/2D templates.
      </p>
      
      <div class="dl-options">
        <a href="https://github.com/kolpa-engine" class="dl-btn dl-btn-primary" target="_blank" rel="noopener noreferrer">
          <svg viewBox="0 0 24 24" width="16" height="16" stroke="currentColor" stroke-width="2.5" fill="none" stroke-linecap="round" stroke-linejoin="round" class="dl-icon"><path d="M3 15v4c0 1.1.9 2 2 2h14a2 2 0 0 0 2-2v-4M7 10l5 5 5-5M12 15V3"/></svg>
          Download for Windows (x64)
        </a>
        <a href="https://github.com/kolpa-engine" class="dl-btn dl-btn-secondary" target="_blank" rel="noopener noreferrer">
          <svg viewBox="0 0 24 24" width="16" height="16" stroke="currentColor" stroke-width="2.5" fill="none" stroke-linecap="round" stroke-linejoin="round" class="dl-icon"><rect x="2" y="2" width="20" height="20" rx="2" ry="2"></rect><line x1="12" y1="18" x2="12" y2="12"></line><line x1="12" y1="6" x2="12.01" y2="6"></line></svg>
          Download for Linux (x64)
        </a>
      </div>
    </div>

    <!-- Compile from Source -->
    <div class="download-card source-card">
      <h2>Build from Source</h2>
      <p class="dl-desc">
        Cloning the repository is best for developers building custom modules or extending the engine's core C++ subsystems.
      </p>
      <div class="code-window">
        <div class="code-window-bar">
          <span>Terminal</span>
          <button class="compile-btn" onclick="copyCode(this)">Copy</button>
        </div>
        <pre class="code-block" style="color: var(--text-muted);"><code><span style="color: var(--accent);">$</span> git clone https://github.com/kolpa-engine/kolpa.git
<span style="color: var(--accent);">$</span> cd kolpa
<span style="color: var(--accent);">$</span> dotnet run --project Source/Editor</code></pre>
      </div>
    </div>
  </div>

  <!-- System Requirements Section -->
  <div class="requirements-section">
    <h2 class="section-title">System Requirements</h2>
    <div class="req-cards-grid">
      <!-- Minimum Requirements Card -->
      <div class="req-card">
        <h3 class="req-card-title">Minimum</h3>
        <div class="req-item">
          <span class="req-item-label">OS</span>
          <span class="req-item-val">Windows 10 (64-bit) / Linux (Vulkan 1.2+)</span>
        </div>
        <div class="req-item">
          <span class="req-item-label">GPU</span>
          <span class="req-item-val">DirectX 12 / Vulkan 1.2 compatible card (Intel HD 530+)</span>
        </div>
        <div class="req-item">
          <span class="req-item-label">CPU</span>
          <span class="req-item-val">Intel Core i3 / AMD Ryzen 3</span>
        </div>
        <div class="req-item">
          <span class="req-item-label">SDK</span>
          <span class="req-item-val">.NET 10.0 runtime / C++20 compiler</span>
        </div>
      </div>

      <!-- Recommended Requirements Card -->
      <div class="req-card req-card-recommended">
        <h3 class="req-card-title">Recommended</h3>
        <div class="req-item">
          <span class="req-item-label">OS</span>
          <span class="req-item-val">Windows 11 (64-bit) / Linux</span>
        </div>
        <div class="req-item">
          <span class="req-item-label">GPU</span>
          <span class="req-item-val">NVIDIA GTX 1060 / AMD RX 580 (Vulkan 1.3 preferred)</span>
        </div>
        <div class="req-item">
          <span class="req-item-label">CPU</span>
          <span class="req-item-val">Intel Core i5 / AMD Ryzen 5 (Multithreaded preferred)</span>
        </div>
        <div class="req-item">
          <span class="req-item-label">SDK</span>
          <span class="req-item-val">.NET 10.0 SDK / MSVC v143 / GCC 12</span>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
/* CSS Styles for Download Page */
.main-download-content {
  padding-top: var(--spacing-lg);
  padding-bottom: var(--spacing-xl);
}

.download-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: var(--spacing-lg);
  margin-bottom: var(--spacing-xl);
}

.download-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: var(--spacing-lg);
  display: flex;
  flex-direction: column;
  position: relative;
  transition: border-color 0.15s ease;
}

.download-card:hover {
  border-color: var(--text-muted);
}

.dl-badge {
  position: absolute;
  top: var(--spacing-md);
  right: var(--spacing-md);
  background: var(--bg-surface);
  border: 1px solid var(--border);
  color: var(--text-muted);
  font-size: 0.65rem;
  font-family: var(--font-mono);
  padding: 2px 6px;
  border-radius: var(--radius);
}

.download-card h2 {
  font-size: 1.1rem;
  font-weight: 700;
  margin-bottom: var(--spacing-sm);
  color: var(--text);
}

.dl-desc {
  font-size: 0.8rem;
  color: var(--text-muted);
  line-height: 1.45;
  margin-bottom: var(--spacing-lg);
}

.dl-options {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm);
  margin-top: auto;
}

.dl-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  font-family: var(--font-sans);
  transition: all 0.15s ease;
  padding: 12px 24px;
  font-size: 0.9rem;
  font-weight: 600;
  border-radius: var(--radius);
  cursor: pointer;
  text-decoration: none;
}

.dl-btn-primary {
  background: var(--text);
  color: var(--bg-deep) !important;
  border: 1px solid var(--text);
}

.dl-btn-primary:hover {
  background: var(--text-muted);
  border-color: var(--text-muted);
  color: var(--bg-deep) !important;
  transform: translateY(-1px);
}

.dl-btn-secondary {
  background: transparent;
  border: 1px solid var(--border);
  color: var(--text-muted) !important;
}

.dl-btn-secondary:hover {
  border-color: var(--text);
  color: var(--text) !important;
  transform: translateY(-1px);
}

.dl-icon {
  flex-shrink: 0;
  transition: transform 0.15s ease;
}

.dl-btn:hover .dl-icon {
  transform: translateY(1px);
}

.source-card .code-window {
  margin-top: auto;
}

/* System Requirements Cards */
.requirements-section {
  margin-top: var(--spacing-xl);
}

.requirements-section .section-title {
  font-size: 1.1rem;
  font-weight: 700;
  margin-bottom: var(--spacing-lg);
  color: var(--text);
}

.req-cards-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: var(--spacing-lg);
}

.req-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: var(--spacing-lg);
  display: flex;
  flex-direction: column;
  gap: var(--spacing-md);
}

.req-card-recommended {
  border-color: var(--border);
}

.req-card:hover {
  border-color: var(--text-dim);
}

.req-card-title {
  font-size: 0.95rem;
  font-weight: 700;
  color: var(--text);
  border-bottom: 1px solid var(--border);
  padding-bottom: var(--spacing-sm);
  margin-top: 0;
  margin-bottom: var(--spacing-xs);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.req-item {
  display: flex;
  justify-content: space-between;
  gap: var(--spacing-md);
  font-size: 0.75rem;
  line-height: 1.45;
}

.req-item-label {
  font-weight: 600;
  color: var(--text-muted);
  min-width: 60px;
  flex-shrink: 0;
}

.req-item-val {
  color: var(--text-dim);
  text-align: right;
}

.req-card:hover .req-item-val {
  color: var(--text-muted);
}
</style>

<script>
function copyCode(btn) {
  const code = btn.closest('.code-window').querySelector('code').innerText;
  navigator.clipboard.writeText(code).then(() => {
    const originalText = btn.innerText;
    btn.innerText = "Copied!";
    setTimeout(() => {
      btn.innerText = originalText;
    }, 2000);
  });
}
</script>
